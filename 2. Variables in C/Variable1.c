#include <stdio.h>//stdio means standard i/o, .h refers to the header file

int main() {

    int age = 25;/*Datatype = integer, variable name = age,
     the variable has been initialized */

    printf("%d", age);//%d is a format specifier used to print variables in C

    return 0;
}